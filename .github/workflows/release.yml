name: release
on:
  push:
    tags: ["v*"]
permissions:
  contents: write
jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "${{ github.ref_name }}" \
            --notes "" \
            --target "${{ github.sha }}"
        env:
          GH_TOKEN: ${{ github.token }}
  build:
    needs: create
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: ""
            name: linux-x64
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: ""
            name: macos-arm64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: .exe
            name: x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - run: cargo build --release --target ${{ matrix.target }}
      - name: upload binary
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          VER="${{ github.ref_name }}"
          NAME="${{ matrix.name }}"
          SUFFIX="${{ matrix.suffix }}"
          SRC="target/${{ matrix.target }}/release/todo${SUFFIX}"
          cp "${SRC}" "todo-${NAME}${SUFFIX}"
          cp "${SRC}" "todo-${NAME}-${VER}${SUFFIX}"
          gh release upload "${VER}" \
            "todo-${NAME}${SUFFIX}" \
            "todo-${NAME}-${VER}${SUFFIX}" \
            --clobber
      - name: install WiX
        if: matrix.os == 'windows-latest'
        run: choco install wixtoolset --no-progress -y
      - name: build msi
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $ver = "$env:GITHUB_REF_NAME" -replace "^v", ""
          & "${env:WIX}bin\candle.exe" wix\main.wxs "-dVersion=$ver" -out todo.wixobj
          & "${env:WIX}bin\light.exe" todo.wixobj "-out" "todo-x64-$env:GITHUB_REF_NAME.msi"
      - name: upload msi
        if: matrix.os == 'windows-latest'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          cp "todo-x64-${{ github.ref_name }}.msi" "todo-x64.msi"
          gh release upload "${{ github.ref_name }}" \
            "todo-x64.msi" \
            "todo-x64-${{ github.ref_name }}.msi" \
            --clobber
